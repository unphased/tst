{
  "extends": "../../../tsconfig.json",
  "include": [
    "./*.ts"
  ],
  "compilerOptions": {
    "lib": [ "dom" ],
    "module": "esnext",
    "target": "es2018",
    "moduleResolution": "bundler",                  /* Specify how TypeScript looks up a file from a given module specifier. */
    // "allowImportingTsExtensions": true,                /* Allow importing .ts files that only contain types. */
  }
}

